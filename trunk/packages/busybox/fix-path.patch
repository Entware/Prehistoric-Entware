diff -urx .svn ../../../downloads/openwrt_trunk-r36151/package/utils/busybox/Makefile ../../../openwrt_trunk/package/utils/busybox/Makefile
--- ../../../downloads/openwrt_trunk-r36151/package/utils/busybox/Makefile	2013-04-27 00:01:38.994379041 +0400
+++ ../../../openwrt_trunk/package/utils/busybox/Makefile	2013-04-27 00:02:08.870377079 +0400
@@ -99,20 +98,53 @@
 		CC="$(TARGET_CC)" \
 		CROSS_COMPILE="$(TARGET_CROSS)" \
 		EXTRA_CFLAGS="$(TARGET_CFLAGS)" \
+		EXTRA_LDFLAGS="$(TARGET_LDFLAGS)" \
 		ARCH="$(ARCH)" \
-		CONFIG_PREFIX="$(PKG_INSTALL_DIR)" \
+		CONFIG_PREFIX="$(PKG_INSTALL_DIR)/opt" \
 		LDLIBS="$(LDLIBS)" \
 		$(BB_MAKE_VERBOSE) \
 		install
 endef
 
 define Package/busybox/install
-	$(INSTALL_DIR) $(1)/etc/init.d
-	$(CP) $(PKG_INSTALL_DIR)/* $(1)/
-	for tmp in $(init-y); do \
-		$(INSTALL_BIN) ./files/$$$$tmp $(1)/etc/init.d/$$$$tmp; \
-	done
-	-rm -rf $(1)/lib64
+	$(INSTALL_DIR) $(1)/opt/{bin,sbin}
+	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/opt/bin/busybox $(1)/opt/bin/
+	$(INSTALL_DIR) $(1)/opt/share
+	(cd $(PKG_INSTALL_DIR); \
+	    find ./opt -lname "*" | sed "s|^./opt/||g" > $(1)/opt/share/busybox_applets.lst.dist)
+endef
+
+define Package/busybox/postinst
+#!/bin/sh
+
+cd /opt
+rm -f share/busybox_applets.lst
+for applet in $$(cat share/busybox_applets.lst.dist)
+do
+    if [ ! -e $$applet ]
+    then
+	ln -s /opt/bin/busybox $$applet
+	echo "$$applet" >> share/busybox_applets.lst
+    else
+	echo "Warning: '/opt/$$applet' is already exists, skipping."
+    fi
+done
+endef
+
+define Package/busybox/postrm
+#!/bin/sh
+
+cd /opt
+for applet in $$(cat share/busybox_applets.lst)
+do
+    if [ -L $$applet ]
+    then
+	rm -f $$applet
+    else
+	echo "Warning: '/opt/$$applet' has been deleted\modified before, skipping."
+    fi
+done
+rm -f share/busybox_applets.lst
 endef
 
 $(eval $(call BuildPackage,busybox))
diff -urx .svn ../../../downloads/openwrt_trunk-r36151/package/utils/busybox/patches/200-etc_crontabs.patch ../../../openwrt_trunk/package/utils/busybox/patches/200-etc_crontabs.patch
--- ../../../downloads/openwrt_trunk-r36151/package/utils/busybox/patches/200-etc_crontabs.patch	2013-04-02 14:27:54.577539966 +0400
+++ ../../../openwrt_trunk/package/utils/busybox/patches/200-etc_crontabs.patch	2013-04-25 12:03:22.496731959 +0400
@@ -5,7 +5,7 @@
  
  #define TMPDIR          CONFIG_FEATURE_CROND_DIR
 -#define CRONTABS        CONFIG_FEATURE_CROND_DIR "/crontabs"
-+#define CRONTABS        "/etc/crontabs"
++#define CRONTABS        "/opt/etc/crontabs"
  #ifndef SENDMAIL
  # define SENDMAIL       "sendmail"
  #endif
@@ -16,7 +16,7 @@
  #include "libbb.h"
  
 -#define CRONTABS        CONFIG_FEATURE_CROND_DIR "/crontabs"
-+#define CRONTABS        "/etc/crontabs"
++#define CRONTABS        "/opt/etc/crontabs"
  #ifndef CRONUPDATE
  #define CRONUPDATE      "cron.update"
  #endif
