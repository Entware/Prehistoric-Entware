--- ../../../openwrt_trunk.orig/feeds/packages/utils/usbutils/Makefile	2011-11-28 10:56:51.000000000 +0400
+++ ../../../openwrt_trunk/feeds/packages/utils/usbutils/Makefile	2011-11-28 14:54:46.275777909 +0400
@@ -27,20 +27,20 @@
   URL:=http://www.linux-usb.org/
 endef
 
-define Package/usbutils/postinst
+#define Package/usbutils/postinst
 #!/bin/sh
-[ -z "$${IPKG_INSTROOT}" ] || \
-(cd $${IPKG_INSTROOT}/usr/share; $${PKG_ROOT}/usr/sbin/update-usbids.sh ; rm usb.ids.gz.old)
-exit 0
-endef
+#[ -z "$${IPKG_INSTROOT}" ] || \
+#(cd $${IPKG_INSTROOT}/opt/share; $${PKG_ROOT}/opt/sbin/update-usbids.sh ; rm usb.ids.gz.old)
+#exit 0
+#endef
 
 define Package/usbutils/install
-	$(INSTALL_DIR) $(1)/usr/bin
-	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/usr/bin/lsusb $(1)/usr/bin/
-	$(INSTALL_DIR) $(1)/usr/sbin
-	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/usr/sbin/update-usbids.sh $(1)/usr/sbin/
-	$(INSTALL_DIR) $(1)/usr/share
-	$(INSTALL_DATA) $(PKG_INSTALL_DIR)/usr/share/usb.ids.gz $(1)/usr/share/
+	$(INSTALL_DIR) $(1)/opt/bin
+	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/opt/bin/lsusb $(1)/opt/bin/
+	$(INSTALL_DIR) $(1)/opt/sbin
+	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/opt/sbin/update-usbids.sh $(1)/opt/sbin/
+	$(INSTALL_DIR) $(1)/opt/share
+	$(INSTALL_DATA) $(PKG_INSTALL_DIR)/opt/share/usb.ids.gz $(1)/opt/share/
 endef
 
 $(eval $(call BuildPackage,usbutils))
