--- ../../../openwrt_trunk.orig/feeds/packages/net/elinks/Makefile	2012-06-12 05:06:14.000000000 +0400
+++ ../../../openwrt_trunk/feeds/packages/net/elinks/Makefile	2012-06-20 14:52:06.000000000 +0400
@@ -9,10 +9,10 @@
 
 PKG_NAME:=elinks
-PKG_VERSION:=0.11.7
+PKG_VERSION:=0.12pre5
 PKG_RELEASE:=1
 
 PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.bz2
 PKG_SOURCE_URL:=http://elinks.or.cz/download
-PKG_MD5SUM:=fcd087a6d2415cd4c6fd1db53dceb646
+PKG_MD5SUM:=92790144290131ac5e63b44548b45e08
 
 include $(INCLUDE_DIR)/package.mk
@@ -21,5 +21,5 @@
   SECTION:=net
   CATEGORY:=Network
-  DEPENDS:=+libopenssl
+  DEPENDS:=+libopenssl +zlib
   TITLE:=A text based web browser
   URL:=http://elinks.or.cz/
@@ -48,16 +48,16 @@
                         --program-prefix="" \
                         --program-suffix="" \
-                        --prefix=/usr \
-                        --exec-prefix=/usr \
-                        --bindir=/usr/bin \
-                        --datadir=/usr/share \
-                        --includedir=/usr/include \
-                        --infodir=/usr/share/info \
-                        --libdir=/usr/lib \
-                        --libexecdir=/usr/lib \
-                        --localstatedir=/var \
-                        --mandir=/usr/share/man \
-                        --sbindir=/usr/sbin \
-                        --sysconfdir=/etc \
+                        --prefix=/opt \
+                        --exec-prefix=/opt \
+                        --bindir=/opt/bin \
+                        --datadir=/opt/share \
+                        --includedir=/opt/include \
+                        --infodir=/opt/share/info \
+                        --libdir=/opt/lib \
+                        --libexecdir=/opt/lib \
+                        --localstatedir=/opt/var \
+                        --mandir=/opt/share/man \
+                        --sbindir=/opt/sbin \
+                        --sysconfdir=/opt/etc \
                         $(DISABLE_NLS) \
                         --enable-small\
@@ -78,14 +78,14 @@
                         --without-libiconv \
                         --without-lua \
-                        --with-openssl="$(STAGING_DIR)/usr" \
+                        --with-openssl="$(STAGING_DIR)/opt" \
                         --without-spidermonkey\
                         --without-x\
-                        --without-zlib \
+                        --with-zlib \
  	);
 endef
 
 define Package/elinks/install
-	$(INSTALL_DIR) $(1)/usr/bin
-	$(INSTALL_BIN) $(PKG_BUILD_DIR)/src/elinks $(1)/usr/bin/
+	$(INSTALL_DIR) $(1)/opt/bin
+	$(INSTALL_BIN) $(PKG_BUILD_DIR)/src/elinks $(1)/opt/bin/
 endef
 
