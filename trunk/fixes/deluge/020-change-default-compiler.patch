--- a/setup.py	2012-01-17 12:24:03.000000000 +0400
+++ b/setup.py	2012-01-17 12:31:43.226499676 +0400
@@ -55,13 +55,13 @@
     return platform.system() == "Darwin"
 
 if not os.environ.has_key("CC"):
-    os.environ["CC"] = "gcc"
+    os.environ["CC"] = "mipsel-uclibc-gcc"
 
 if not os.environ.has_key("CXX"):
-    os.environ["CXX"] = "gcc"
+    os.environ["CXX"] = "mipsel-uclibc-gcc"
 
 if not os.environ.has_key("CPP"):
-    os.environ["CPP"] = "g++"
+    os.environ["CPP"] = "mipsel-uclibc-g++"
 
 # The libtorrent extension
 _extra_compile_args = [
