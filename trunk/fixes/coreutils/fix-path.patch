--- ../../../backup/trunk_clean/feeds/packages/utils/coreutils/Makefile	2011-11-28 10:56:52.000000000 +0400
+++ ../../../trunk/feeds/packages/utils/coreutils/Makefile	2011-12-01 14:03:51.006150232 +0400
@@ -20,7 +20,7 @@
 
 COREUTILS_APPLETS:=\
 	base64 basename cat chcon chgrp chmod chown chroot cksum comm cp \
-	csplit cut date dd dir dircolors dirname du echo env expand expr \
+	csplit cut date dd df dir dircolors dirname du echo env expand expr \
 	factor false fmt fold groups head hostid id install join kill link \
 	ln logname ls md5sum mkdir mkfifo mknod mktemp mv nice nl nohup nproc \
 	od paste pathchk pinky pr printenv printf ptx pwd readlink rm rmdir \
@@ -67,7 +67,8 @@
 CONFIGURE_VARS += \
 	gl_cv_func_mbrtowc_incomplete_state=yes \
 	gl_cv_func_mbrtowc_retval=yes \
-	gl_cv_func_wcrtomb_retval=yes
+	gl_cv_func_wcrtomb_retval=yes \
+	fu_cv_sys_stat_statfs2_bsize=yes
 
 ifneq ($(CONFIG_USE_UCLIBC),)
   CONFIGURE_VARS += \
@@ -90,8 +91,8 @@
 
 define BuildPlugin
   define Package/$(1)/install
-	$(INSTALL_DIR) $$(1)/usr/bin
-	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/usr/bin/$(2) $$(1)/usr/bin/
+	$(INSTALL_DIR) $$(1)/opt/bin
+	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/opt/bin/$(2) $$(1)/opt/bin/
   endef
 
   $$(eval $$(call BuildPackage,$(1)))
