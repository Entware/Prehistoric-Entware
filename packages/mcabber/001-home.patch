--- a/mcabber/settings.c	2010-12-01 23:51:21.000000000 +0300
+++ b/mcabber/settings.c	2012-07-24 00:48:20.000000000 +0400
@@ -113,7 +113,8 @@
       return -1;
     }
 
-    home = getenv("HOME");
+    //home = getenv("HOME");
+    home = "/opt/etc";
     if (!home) {
       scr_LogPrint(LPRINT_LOG, "Can't find home dir!");
       fprintf(stderr, "Can't find home dir!\n");
