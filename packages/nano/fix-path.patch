--- ../../../downloads/openwrt_trunk-r42803/feeds/packages/utils/nano/Makefile	2014-10-06 15:46:23.777250645 +0400
+++ ../../../openwrt_trunk/feeds/packages/utils/nano/Makefile	2014-10-08 14:03:44.482715001 +0400
@@ -36,18 +36,22 @@
 endef
 
 CONFIGURE_ARGS += \
-	--enable-tiny \
 	--disable-glibtest \
 	--disable-utf8 \
         --without-slang \
-        --disable-color \
-
-CONFIGURE_VARS += \
-	ac_cv_header_regex_h=no \
+        --enable-wrapping \
+        --disable-help \
+        --disable-justify \
+        --disable-mouse \
+        --disable-operatingdir \
+        --disable-speller \
+        --disable-tabcomp \
+        --disable-browser \
+        --enable-color \
 
 define Package/nano/install
-	$(INSTALL_DIR) $(1)/usr/bin
-	$(CP) $(PKG_INSTALL_DIR)/usr/bin/$(PKG_NAME) $(1)/usr/bin/
+	$(INSTALL_DIR) $(1)/opt/bin
+	$(CP) $(PKG_INSTALL_DIR)/opt/bin/$(PKG_NAME) $(1)/opt/bin/
 endef
 
 $(eval $(call BuildPackage,nano))
